spring.application.name=airport-map-backend
server.port=8080

# Keep the locally crawled 188-place dataset as the data source.
# Relative to the folder you run the backend from (Back-End/ when using mvnw).
app.data-file=data/airport-locations.json
