spring.application.name=airport-map-backend
server.port=8080

# JSON file that stores all locations (read at startup, rewritten after every add/edit/delete).
# Relative to the folder you run the backend from (Back-End/ when using mvnw).
app.data-file=data/locations.json
